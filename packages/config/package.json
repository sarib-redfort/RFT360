{
  "name": "@rft360/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript configuration bases for the RFT360 monorepo",
  "files": [
    "tsconfig"
  ],
  "scripts": {
    "lint": "echo \"no lint for config package\"",
    "typecheck": "echo \"no typecheck for config package\"",
    "clean": "echo \"nothing to clean\""
  }
}
